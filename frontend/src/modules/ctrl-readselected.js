const options = {
  innerHTML: `<label for="readSelected" title="Read the selected paragraph\n(Play|Pause">
    <span>||></span>
    <button
    id="readSelected" class="hidden"
    onclick="window.RE.read()"
    >
    </label>`
};
